# TDS Kit Visualizer Server Configuration

# Express REST API & WebSocket Port (shared on same HTTP server)
PORT=5000

# MongoDB Connection URI (Local MongoDB or MongoDB Atlas)
MONGODB_URI=mongodb://127.0.0.1:27017/tds_kit

# Client CORS origin (Vite default is http://localhost:5173)
CLIENT_URL=http://localhost:5173

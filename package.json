{
  "name": "tds-kit-visualizer",
  "version": "1.0.0",
  "description": "Full-stack MERN visualizer for real-time ESP32 hardware data structures",
  "scripts": {
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "simulate": "cd server && npm run simulate"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
